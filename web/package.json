{
  "name": "projectz-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:ghpages": "tsc && vite build --outDir ../gh-pages --base=/projectz/",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "preact": "^10.22.0",
    "preact-router": "^4.1.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
